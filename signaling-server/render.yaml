services:
  - type: web
    name: remotelink-signaling
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: GROQ_API_KEY
        sync: false # Set your Groq API Key here in the Render dashboard
